# fastapi-test

## Start 
- Python >= 3.8

```sh
$ cd fastapi 
$ python -m venv venv
$ venv/bin/pip install -r requirements.txt 

$ ./start.sh 
```

```sh
$ cd flask 
$ python -m venv venv
$ venv/bin/pip install -r requirements.txt 

$ ./start.sh 
```

